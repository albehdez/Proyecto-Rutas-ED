package Application;

/**
 * MapController
 */
public class MapController {

    // private double zoom = 1.0;
    // private double translateX = 0.0;
    // private double translateY = 0.0;

    // public void drawMap(GraphicsContext gc) {

    // }

    // public void zoom(ScrollEvent event) {
    // zoom += event.getDeltaY() / 1000.0;
    // zoom = Math.max(zoom, 0.1);
    // zoom = Math.min(zoom, 10.0);
    // gc.setTransform(zoom, 0, 0, zoom, translateX, translateY);
    // event.consume();
    // }

    // public static void drawMap(GraphicsContext gc) {

    // gc.setFill(Color.valueOf("#E3E1D2"));
    // // gc.fillRect(0, 0, 100, 100);
    // Canvas canvas = gc.getCanvas();
    // gc.fillRect(0, 0, canvas.getWidth(), canvas.getHeight());
    // // gc.setStroke(Color.GREEN);
    // // gc.setLineWidth(3);
    // // gc.strokeLine(50, 50, 150, 150);
    // }
}